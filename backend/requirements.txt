# SIMPLIFIED REQUIREMENTS - BUILD DETECTRON2 FROM SOURCE
# requirements.txt

# Web Framework
Flask==2.3.3
Flask-CORS==4.0.0
gunicorn==21.2.0
python-dotenv==1.0.0

# Core Dependencies (PyTorch installed separately in build command)
numpy>=1.20.0,<1.27.0
opencv-python-headless>=4.8.0
Pillow>=10.0.0

# Detectron2 dependencies
setuptools>=40.0.0
wheel
packaging
pyyaml>=5.1
scipy
matplotlib
termcolor
tabulate
cloudpickle
omegaconf>=2.1
hydra-core>=1.1
black
isort
flake8
psutil


